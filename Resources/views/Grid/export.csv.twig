{% set format = 'Y-m-d' %}{% set first = true %}{% for header in headers %}{% if not first %},{% else %}{% set first = false %}{% endif %}"{{ header.title }}"{% endfor %}{{ "\r\n" }}{% set first = true %}{% for entity in entities %}{% if not first %}{{ "\r\n" }}{% else %}{% set first = false %}{% endif %}{% set first = true %}{% for header in headers %}{% if not first %},{% else %}{% set first = false %}{% endif %}"{% if 'date' == header.type %}{{ entity.values[header.column] | date(format) }}{% else %}{{ entity.values[header.column] }}{% endif %}"{% endfor %}{% endfor %}